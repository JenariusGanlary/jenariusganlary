# public/

Static assets served as-is at the site root.

## d57a05759ee544d4be6afa531dc1d30a.txt

A domain-verification file. The bare-hex filename (no `google` or `bing`
prefix) doesn't clearly match Google Search Console's usual
`google<code>.html` convention or Bing's `BingSiteAuth.xml` convention, so
the exact service this verifies hasn't been confirmed as of this writing.

**Before deleting this file:** check whether removing it breaks verification
in Search Console, Bing Webmaster Tools, or any other service dashboard.
Domain-verification files generally cause silent loss of verified status
(not a build error) if deleted while still relied upon, so confirm in the
relevant dashboard first rather than assuming it's safe to remove.

If you confirm what this verifies, update this note — this is meant to stop
the next "can I delete this?" guess, not encode a real answer yet.